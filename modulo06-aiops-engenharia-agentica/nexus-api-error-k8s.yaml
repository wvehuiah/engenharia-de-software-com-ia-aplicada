apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexus-api-error
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nexus-api-error
  template:
    metadata:
      labels:
        app: nexus-api-error
    spec:
      containers:
      - name: nexus-api-error
        image: nginx:latest
        ports:
        - containerPort: 80
        readinessProbe:
          httpGet:
            path: /
            port: 80
---
apiVersion: v1
kind: Service
metadata:
  name: nexus-api-error-svc
spec:
  selector:
    app: nexus-api-error
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
