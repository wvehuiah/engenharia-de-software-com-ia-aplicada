apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexus-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nexus-api
  template:
    metadata:
      labels:
        app: nexus-api
    spec:
      containers:
      - name: nexus-api
        image: nginx:latest
        ports:
        - containerPort: 80
        readinessProbe:
          httpGet:
            path: /
            port: 80
---
apiVersion: v1
kind: Service
metadata:
  name: nexus-api-svc
spec:
  selector:
    app: nexus-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
